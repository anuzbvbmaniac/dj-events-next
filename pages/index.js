import Layout from "@/components/Layout";
import Showcase from "@/components/Showcase";

const HomePage = () => {
    return (
        <Layout>
            <Showcase />
        </Layout>
    );
};

export default HomePage;
